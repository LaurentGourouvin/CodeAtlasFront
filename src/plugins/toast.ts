import type ToastConfig from "@/types/ToastConfig";

export const ToastConfiguration: ToastConfig = {
    position: 'top-right',
    timeout: 3000,
    closeOnClick: true,
    pauseOnHover: true
}

export default ToastConfiguration;